#pragma once

/**
 * This header contains the OpenGL includes which are needed on SDL platforms. This file is
 * also include by the common code base. The same file also exists for android.
 */

#include <GL/glew.h>
#include <SDL2/SDL_opengl.h>

